workflows:
  android-workflow:
    name: Android Build
    environment:
      groups: []
    scripts:
      - name: Build Android APK
        script: |
          cd finance-app
          flutter pub get
          flutter build apk --release
    artifacts:
      - finance-app/build/app/outputs/flutter-apk/*.apk
